# 오늘의 포켓몬은 뭘까요 🔴

포켓몬 이름을 입력해서 도감을 채우는 퀴즈 게임.

- 전 세계 포켓몬 **1025종** (메가진화 · 리전폼 · 거다이맥스 포함 1250칸)
- 이름을 맞히면 해당 포켓몬의 모든 폼이 한꺼번에 열림
- 세대별 · 타입별 · **도감별(18종)** 필터, 실루엣 모드, 모습 자동 전환(한 포켓몬의 여러 폼 자동 순환) 지원
- 정답 입력칸 옆 지우기 버튼(←)으로 입력값 전체 삭제 가능
- 칸을 길게눌러 이름 공개/재공개(토글) 가능 — 공개 시에는 확인창("해당 칸의 포켓몬 이름을 공개할까요?")이 뜨고, 이미 풀린 칸을 다시 길게누르면 확인창 없이 바로 미발견 상태로 되돌아감(스크롤 중 오조작으로 인한 스포일러 방지 목적)
- 진행 기록은 브라우저(localStorage)에 자동 저장

## 실행

`index.html` 파일 하나로 완전히 독립 동작. 모든 이미지(1,498장)가 파일 안에 포함되어 있어 인터넷 연결 없이도 실행된다.

## GitHub Pages로 배포

1. 이 폴더 내용을 저장소에 업로드
2. 저장소 **Settings → Pages → Branch: main / (root)** 선택 후 저장
3. 잠시 후 `https://<계정명>.github.io/<저장소명>/` 에서 플레이

---

## 개발 노트 (이어서 작업할 때 참고)

원본 v2/v3/v4는 삭제됨(2026-07-07). `github-upload/index.html`이 **유일한 원본**이다.

### 미해결 이슈

- **PC IME 잔여 글자**: 정답 처리 후 입력칸을 새 원소로 교체하는 방식으로 대부분 해결했으나, 일부 환경에서 첫 글자 소실이 재현된다는 보고 있음. 다음 작업 시 입력 이벤트 로깅으로 원인 추적 필요.

### 최근 변경 (2026-07-11)

- 정답 입력칸 오른쪽에 지우기 버튼(←, 빨간 계열) 추가 — 클릭 시 입력값 전체 삭제
- `doLongPress`에 확인창 추가 — 칸을 길게눌러 이름을 **공개**할 때만 `confirm()`으로 한 번 확인. 스크롤 중 실수로 길게눌려 스포일러가 뜨는 사고 방지
- 이미 풀린 칸(길게눌러 공개했든 직접 타이핑해서 맞혔든 무관)을 다시 길게누르면 확인창 없이 바로 미발견 상태로 되돌아가도록 토글화 (기존엔 `done` 클래스 가드 때문에 사실상 동작 안 하던 죽은 코드였음)

### 2026-07-09

- 도감 버전 토글(RG/LGPE, GS/HGSS, RS/ORAS, DP/Pt, BW/B2W2, SM/USUM 등 6개 지역) 추가 — 지역도감 제목 옆 버전 버튼으로 전환, 목록·카운터·도감번호 자동 재계산
- PC IME 문제 대응으로 정답 시 입력칸 교체 방식 도입 (위 미해결 이슈 참고)
- 기록 보기 문구 다듬음

### 2026-07-07

- 칼로스도감을 센트럴/코스트/마운틴 3섹션 통합 구조로 병합, 합집합 457종 카운터
- 관동(LGPE 153) · 호연(ORAS 211) · 하나(0000번부터 시작) 등 지역도감 목록·번호 규칙 정리
- 메가진화 전용 도감 모드(95종, 엔트리 단위 카운트) 추가
- 로그인 동기화를 "합집합"에서 "서버 우선"으로 변경 (비로그인 진행분은 로그인 시 서버 기준으로 덮임에 유의)
- 자동완성 오작동 · 파비콘 손상 · 도감별 필터·카운터 버그 등 수정
- 실루엣/타입 미리보기 토글에 기록 보호 확인창 추가
- 리전폼 정렬, 알로라도감(ALOLA_NATDEX) 추가 등 도감 데이터 정비

### 핵심 데이터 구조 (단일 `<script>` 내)

- `DATA` — 전체 엔트리 배열. `[이름, 국가도감번호(0-index), 타입1, 타입2, 박스번호, 이미지ID배열, ...]`
  - `e[1]` = 국가도감번호-1, `e[4]` = 박스(세대 구간) 인덱스
- `BOXES` — 박스 정의: 0~5=1~6세대, 6/7=메가XY/ORAS, 8=7세대, 9=세대불명, 10=8세대SwSh, 11=거다이맥스, 12=8세대LA, 13/14=9세대(본편/DLC), 15/16=메가LZA
- `mb(b)` — 박스 병합(14→13, 16→15), `DISP` — 박스 표시 순서
- `DEXDEF` — 도감 버튼 정의 `[이름, 필터함수]` (배열 20개, 메뉴 표시는 18개). 인덱스: 0전국 1관동 2성도 3호연 4신오 5하나(번호 0000부터) 6칼로스(통합·3섹션, 7·8은 구 코스트/마운틴으로 메뉴 숨김+dexIdx 리맵) 9알로라 10가라르 11갑옷섬 12왕관설원 13히스이 14팔데아 15북신 16블루베리 17미르 18이차원 19메가진화(전용 모드, MEGA_DEXNO 번호·엔트리 단위 카운트)
- `*_NATDEX` — 각 지역도감의 공식 종 목록(국가도감번호 1-index, 도감 등재 순서). 출처: 성도=HGSS, 호연=RSE, 신오=Pt(210), 하나=B2W2(301), 칼로스=XY 3분할, 가라르/갑옷섬/왕관설원/히스이=Bulbapedia, 알로라/팔데아/북신/블루베리/미르/이차원=PokeAPI
- `DEXORDER` — `{DEXDEF인덱스: NATDEX.map(n=>n-1)}`. 이 목록이 있으면 도감 구성·순서·번호 모두 이걸 따름. 없는 도감(메가진화)만 DEXDEF 필터 사용
- `VARIDX` — 리전폼/메가/거다이맥스 변형 엔트리의 표시 토글(cfg.regional/mega/gmax) 매핑

### 도감별 종 수 검증값 (전수 확인 완료)

관동 151/153(RG/LGPE) · 성도 251/256(GS/HGSS) · 호연 202/211(RS/ORAS) · 신오 151/210(DP/Pt) · 하나 156/301(BW/B2W2, 0000부터) · 칼로스 457(센트럴 153·코스트 153·마운틴 151, 섹션별 독립 번호) · 알로라 302/403(SM/USUM) · 가라르 400 · 갑옷섬 211 · 왕관설원 210 · 히스이 242 · 팔데아 400 · 북신 200 · 블루베리 243 · 미르 232 · 이차원 132

### 파일 위치

- 작업 폴더(로컬): `C:\Users\iruro\Desktop\래성\포켓몬`
- 유일한 원본 = 배포본: `github-upload\index.html`
- 파일이 ~2.4MB로 커서(이미지 base64 내장) 일부 도구에서 줄 단위 읽기가 무거울 수 있음. 데이터/로직은 앞부분 `<script>`에 몰려 있고, 뒤쪽은 대부분 base64 이미지.
